//number - число
//string -строка
//boolean - логический
//null - пустота (значение отсутвует )
//undefined - неопределенность (значение не опеределено)
//bigint - большое целое число
//symbol - уникальный идентификатор (символ). нужен для обьектов

//immutable - неизменяемый (данные не могут быть изменены, они могут быть удалены ирп записаны новые данные)

//object - объект (составной тип данных)

//typeof - оператор, который определяет тип данных
const data = 1;

//.toString - число в строку
//Строку в числов  Number(str)

//.parseIns(str) Строку в целочисленное число  Number(str) собирает слева направо значение
//console.log(Number.parseIns(str);

//.parseFloat(str) Строку в дробное число  Number(str) собирает слева направо значение
//console.log(Number.parseFloat(str);

// const = +prompt("enter the number"); сразу при вводе преобразует в число
//console.log(typeof data); //number

//const million = 1_000_000;
//console.log(million);
//const million = 1e6;
//console.log(million);

//const million = 1e-7;
//console.log(million);

//console.log(0);
//console.log(-0);

// console.log(50/0);

//Math.round() -округление
//Math.floor() -округление в меньшую сторону
//Math.ceil() -округление в большую сторону
//Math.trunc() -выбрасывает дробную часть
//.toFixed() - округляет для количества знаков но возвроащает строку
//Math.trunc(num*100)/100 - округление до 2 знаков после запятой

//Math.max() - находит максимальное значение
//Math.min() - находит минимальное значение
//Math.random() - случайное число от 0 до 1

//\n - перенос строки

//String() - превращает в строку

//.length - длинна строки
//autoboxing



